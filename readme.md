# Dev Setup - npm global packages

## Install
npm i -g pyright sql-language-server tree-sitter-cli

## Purpose
- pyright — Python LSP
- sql-language-server — SQL LSP
- tree-sitter-cli — Treesitter parser builder
